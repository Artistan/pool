import { Component } from '@angular/core';

@Component({
  selector: 'app-about',
  template: `
    <section class="container py-4">
      <h1 class="h3">About</h1>
      <p class="text-muted mb-0">A minimal Angular + Bootstrap + Font Awesome demo deployed to GitHub Pages.</p>
    </section>
  `,
})
export class AboutComponent {}
